#include <iostream>

using namespace std;


int main(){

    int h, e, a, o, w, x;
    cin >> h >> e >> a >> o >> w >> x;

    if( (h + a + e + x) >= (o + w) ){
        cout << "Middle-earth is safe." << endl;
    } else {
        cout << "Sauron has returned." << end;
    }

    return 0;
}